server:
  port: 9002
  context-path: /
spring:
  application:
    name: scloud-eureka-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9002/eureka
